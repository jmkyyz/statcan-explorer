services:
  - type: web
    name: lobbyist-explorer
    runtime: python
    rootDir: lobbyist
    buildCommand: pip install -r requirements.txt && python -u build_db.py
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 10000
