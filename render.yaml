services:
  - type: web
    name: statcan-explorer
    runtime: python
    buildCommand: pip install -r requirements.txt && cd ev-dashboard && npm install && npm run build && cd ..
    startCommand: python proxy.py
    envVars:
      - key: PORT
        value: 10000
